# SPEC: [Phase N — 이름]

> Claude가 이 작업을 진행하는 **단일 기준**이다. 자기완결적으로 작성한다.
> 파생: `docs/ROADMAP.md`의 Phase N. 규칙: 루트 `CLAUDE.md`. 절차: `docs/WORKFLOW.md`.
> 작성 팁: "AskUserQuestion 도구로 나를 인터뷰해서 이 SPEC을 완성해라"로 시작하면 엣지 케이스까지 끌어낸다.

## 1. 목표 (한 문장)
[무엇을, 왜]

## 2. 범위
- **포함**: [...]
- **제외 (Out of scope)**: [...]   ← 명시 중요

## 3. 영향 파일 / 인터페이스
- `src/forge3d/[...]` — [무엇을]
- 인터페이스/시그니처: `[함수·타입 정의]`
- 의존성: [기존 허용 패키지만 / 새로 필요하면 무엇·왜 — 외부 물리엔진 금지]
- 따라야 할 기존 패턴: `[참고 파일]`

## 4. 구현 작업 (체크리스트 — 순서대로, 검증 가능한 단위)
- [ ] **T1.** [작업] — 완료 조건: [...]
- [ ] **T2.** [작업] — 완료 조건: [...]

> 각 task 완료 시: 체크 + `docs/PROGRESS.md` 갱신 + 아래 검증 통과.

## 5. 엣지 케이스 / 제약
- [...]

## 6. 검증 (End-to-end — Phase 게이트)
- **단위/기능 테스트**: [무엇을 덮는가]
- **물리 정확성**(해당 시): 보존법칙 / 해석해 대조 / 기준엔진 대조(`validation/`) / 백엔드 일치(np==jnp).
- **렌더러 계약**(렌더 관련 시): 동일 `SceneSnapshot`이 두 렌더러에서 일관(실시간=미리보기, HQ=골든 이미지). 물리 코어가 `render`를 import하지 않음.
- **API 사용성**(공개 API 관련 시): `examples/`가 라이브러리 내부 접근 없이 15줄 내로 동작.
- **백엔드**: `ENGINE_BACKEND=numpy pytest ...` 와 `ENGINE_BACKEND=jax pytest ...` 둘 다 통과.
- **통과 기준**: 실행 명령 `[...]` → 기대 결과 `[...]`

## 7. 완료 후 리뷰
- 서브에이전트로 diff를 이 SPEC과 대조, 정확성/요구사항 누락만 보고 → 수정 → 커밋·PR.
